const APP_CONFIG = {
  name: 'Artist Website',
  sidebarTitle: 'Studio Manager',
  eyebrow: 'Artist Website'
};

/**
 * @returns {{ name: string, sidebarTitle: string, eyebrow: string }}
 */
function getAppConfig() {
  return APP_CONFIG;
}
